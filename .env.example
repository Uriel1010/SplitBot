# SplitBot Environment Example
# Copy this file to .env and fill real secrets. Do NOT commit your real .env.

# REQUIRED
TELEGRAM_BOT_TOKEN=123456789:REPLACE_WITH_REAL_TOKEN

# OPTIONAL
# Default base currency for new chats (locked after first expense in each chat)
DEFAULT_CURRENCY=USD

# Gemini AI (leave empty to disable AI parsing; regex fallback still works)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash

# AI Provider selection:
# Set AI_PROVIDER to one of: GEMINI, OLLAMA
# If not set but GEMINI_API_KEY present -> GEMINI
# If set to OLLAMA configure OLLAMA_BASE_URL (e.g. http://192.168.1.50:11434) and optional OLLAMA_MODEL
AI_PROVIDER=
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen3:8b

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Future placeholders (uncomment as features land)
# ENABLE_LANG_TOGGLE=true
# ENABLE_JSON_MIGRATION_ON_START=true
